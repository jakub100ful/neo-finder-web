{
  "name": "neo-finder-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "preview": "vite preview",
    "refresh:discover": "node scripts/refresh-discover-data.mjs",
    "refresh:pds-catalogue": "node scripts/refresh-pds-catalogue.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json"
  },
  "dependencies": {
    "three": "latest"
  },
  "devDependencies": {
    "@sveltejs/adapter-static": "latest",
    "@sveltejs/kit": "latest",
    "@sveltejs/vite-plugin-svelte": "latest",
    "svelte": "latest",
    "svelte-check": "latest",
    "vite": "latest"
  }
}
